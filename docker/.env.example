# EngHub 部署环境变量模板
# 复制为 docker/.env 并填入真实值；docker/.env 已被 gitignore，不会提交。

# AI 助手 -> litellm 网关配置
# 网关地址（默认走 model-stack 内网直连）
LLM_GATEWAY_URL=http://model-gateway:4000
# 网关 API Key（必填，从模型网关获取）
LLM_API_KEY=
# 默认使用的模型
LLM_MODEL=openai/qwen-plus
