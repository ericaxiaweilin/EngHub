# 📄 PP生产计划模块业务需求规格说明书

**版本号**: 1.0  
**编制日期**: 2026-07-28  
**状态**: 草案（待评审）

---

## 1. 业务背景与目标

### 1.1 业务背景
当前MES系统已具备工单执行、报工、仓储等核心业务能力，但缺少**上游计划驱动**。生产部门依赖人工经验排产，无法实现"以销定产"的精准计划管理，导致：
- 物料采购滞后或超量
- 产能分配无序，瓶颈工序积压严重
- 订单交期承诺缺乏依据

### 1.2 业务目标
建立**MPS+MRP双层计划体系**，实现：
- ✅ **主生产计划（MPS）**：根据销售订单和预测，制定产品级主计划
- ✅ **物料需求计划（MRP）**：基于BOM展开，计算原材料需求并生成采购建议
- ✅ **产能负荷分析**：提前识别瓶颈工序，避免过度承诺
- ✅ **计划联动**：下达的计划自动转化为MES工单，形成闭环

---

## 2. 业务流程全景图

```
┌─────────────────┐     ┌──────────────┐     ┌───────────────┐     ┌──────────────┐
│  销售订单       │────▶│  M主生产计划 │────▶│   物料需求    │────▶│   采购建议   │
│  销售预测       │     │  (MPS)       │     │    (MRP)      │     │  (请购/采购) │
└─────────────────┘     └──────┬───────┘     └───────┬───────┘     └──────┬───────┘
                                 │                    │                      │
                         ┌───────▼───────┐    ┌──────▼──────┐          ┌──────▼──────┐
                         │  产能负荷分析 │    │ BOM展开     │          │ 供应商比价  │
                         │  (APS/CLC)    │    │ (从MES/ERP) │          │ 获取价格    │
                         └───────┬───────┘    └──────┬──────┘          └──────┬──────┘
                                 │                    │                      │
                         ┌───────▼───────┐    ┌────▼──────┐          ┌──────▼──────┐
                         │  计划冲突检测 │◀────│ 库存可用量│          │  下单执行   │
                         │ (物料/设备)   │    │ (WMS查询) │          │  采购申请   │
                         └──────────────┘    └───────────┘          └──────────────┘
                                 │
                         ┌───────▼───────┐
                         │  MES工单下发  │
                         │ (自动转工单)  │
                         └───────────────┘
```

---

## 3. 核心功能详述

### 3.1 主生产计划（MPS）

#### 3.1.1 业务角色
| 角色 | 职责 |
|------|------|
| 生产计划员 | 创建、修改、确认MPS计划 |
| 生产总监 | 审批重大计划变更 |
| 销售经理 | 查看已确认计划的可交付性 |

#### 3.1.2 计划创建流程

**输入数据：**
- 销售订单（含客户等级、需求日期、数量）
- 销售预测（按品类滚动预测）
- 现有库存状态（从WMS获取）
- 在制品进度（从MES获取）

**业务规则：**
1. **优先级计算公式**：`priority_score = due_date_score + customer_level_score + base_priority`
   - `due_date_score`: 距离需求日期的紧迫度（100分制，越近越高）
   - `customer_level_score`: VIP=50, A=35, B=20, C=10
   - `base_priority`: 计划员手动调整（0-50分）

2. **排程顺序**: 按 `priority_score` 降序排列，交期优先

3. **计划状态流转**: DRAFT → CONFIRMED → RELEASED → IN_PROGRESS → COMPLETED/CANCELLED

#### 3.1.3 计划审批流

```
[计划员创建草稿] → [提交审批] → [生产总监审核] 
        ↓                                ↗
    (可直接下达)                    [拒绝→返回修改]
```

- **无需审批的场景**：常规MPS计划（预测驱动）、紧急插单（需记录备案）
- **必须审批的场景**：计划总量超月度产能80%、涉及新产品/新工艺、客户需求变更超过±10%

#### 3.1.4 产能负荷分析（输入字段说明）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| station_id | string | 工站ID（如STA-ASSY-01） |
| from_date | date | 分析起始日期 |
| to_date | date | 分析截止日期 |
| factory_id | string | 工厂编码 |

**输出指标：**
- `total_capacity_hours`: 期间总可用工时 = CPH × 每日工作小时数 × 工作日数
- `allocated_hours`: 已占用工时（来自已释放工单的工序工时）
- `available_hours`: 剩余可用工时
- `utilization_rate`: 负荷率 = allocated / total × 100%
- `overloaded_dates`: 负荷超过90%的日期列表
- `bottleneck_stations`: 所有工站中负荷最高的前3个

### 3.2 物料需求计划（MRP）

#### 3.2.1 业务场景
MRP是MPS的执行延伸——将**产品级需求**逐层分解为**原材料需求**。

**触发时机：**
- MPS计划确认后（自动触发）
- 手工执行（计划员主动点击"运行MRP"按钮）
- 计划数量变更超过20%时（重新计算）

#### 3.2.2 MRP计算逻辑流程

```
开始
  │
  ▼
[获取MPS计划中的产品和数量]
  │
  ▼
[BOM展开：递归获取所有子部件] ←─┐
  │                              │
  ▼                              │
[查询各层级物料的毛需求]         │
  │                              │
  ▼                              │
[扣除在库可用量 + 在途量] ←───┐   │
  │                           │   │
  ▼                           │   │
[计算净需求量]                │   │
  │                           │   │
  ▼                           │   │
[判断是否短缺] ◄───┐          │   │
  │                  │         │   │
 短缺? ──► [生成采购建议] ◄────┘   │
  │                             │
 无短缺? ────────────────────────┘
  │
  ▼
[输出MRP报告]
结束
```

#### 3.2.3 BOM展开业务规则

| 规则项 | 说明 |
|--------|------|
| BOM来源 | 从MES工艺路线模块获取（routing表关联product_id） |
| 版本控制 | 默认启用版本的BOM；支持指定版本（v1/v2...） |
| 多层展开 | 递归展开至最低采购件（level N） |
| 替代料处理 | 若有BOM中定义的替代料，按比例计入 |
| 废品系数 | 考虑工艺良率损耗（如：工艺良率95%，则需求×1/0.95） |

**示例**：生产100台A产品，BOM结构如下：
```
A产品 (1台)
├─ B部件 (2个/台) × 100 = 200个
│  ├─ 螺丝C (5个/个) × 200 = 1000个
│  └─ 芯片D (1个/个) × 200 = 200个
└─ E外壳 (1个/台) × 100 = 100个
```

#### 3.2.4 净需求计算

```
毛需求 = 产品数量 × BOM用量（含良率补偿）
可用量 = 库存实物 - 预留(在订单/工单中占用)
在途量 = 已下单未到货的采购量
净需求 = 毛需求 - 可用量 - 在途量
```

**安全库存保护**：当 `可用量 < safety_stock` 时，将差额视为额外需求。

#### 3.2.5 采购建议生成

| 字段 | 说明 |
|------|------|
| material_code | 物料编码 |
| shortage_qty | 短缺数量（毛需求-可用） |
| suggested_qty | 建议采购量（考虑MOQ/EOQ） |
| suggest_date | 建议采购日期（需求日期 - 采购提前期） |
| priority | 优先级（URGENT/HIGH/NORMAL/LOW） |
| estimated_cost | 预估金额（建议 qty × 最近采购价） |
| supplier_id | 首选供应商（从物料主数据取） |

**批量规则示例**：
- MOQ（最小订货量）：按物料主数据定义
- EOQ（经济批量）：可按公式 √(2×年需求×订货成本/持有成本) 简化计算
- 包装倍数：物料采购单位（如箱、托盘）的整数倍

#### 3.2.6 MRP报告输出

```
MRP报告单号：MRP-FACTORY-2026-001
计划日期：2026-07-28
计划对象：MPS-FACT-2026-30（产品P-PROD-A）

┌────────────┬─────────┬───────┬───────┬───────┬──────────┬──────────┐
│ 物料编码   │ 名称    │ 毛需  │ 可用  │ 在途  │ 净需求   │ 建议采购 │
├────────────┼─────────┼───────┼───────┼───────┼──────────┼──────────┤
│ RES-10K    │ 电阻10K │ 10000 │ 2000  │ 3000  │ 5000     │ 5000     │
│ CAP-100NF  │ 电容100│ 5000  │ 1000  │ 0     │ 4000     │ 4000     │
│ CHIP-MCU   │ 微控制器│ 200   │ 50    │ 100   │ 50       │ 50       │
└────────────┴─────────┴───────┴───────┴───────┴──────────┴──────────┘

合计短缺价值：¥125,000
建议采购单据数：3份
```

### 3.3 计划与MES联动

#### 3.3.1 工单自动生成业务规则

|MPS计划状态变化 | MRP结果 | 动作 |
|---------------|---------|------|
| RELEASED（已下达） | 有短缺 | 推送采购建议至采购模块 + 自动转工单至MES |
| RELEASED（已下达） | 无短缺 | 仅自动转工单至MES |
| CONFIRMED（已确认） | — | 不触发，仅供预览 |

#### 3.3.2 工单转换映射

| MPS字段 | MES工单字段 | 说明 |
|---------|-------------|------|
| product_id | product_id | 直接映射 |
| quantity | planned_qty | 总计划数量 |
| required_date | due_date | 交付日期 |
| sales_order_id | order_ref | 引用销售订单 |
| plan_code | work_order_code | 前缀"MPS-" + 序列号 |
| factory_id | factory_id | 同工厂 |

---

## 4. 业务约束与规则

### 4.1 数据一致性规则
- MPS计划的产品必须在MES产品主数据中存在
- 计划的生产工厂必须在MES工站/设备范围内
- MRP计算使用的BOM必须是启用状态且有效期覆盖计划日期

### 4.2 权限控制
| 角色 | MPS操作 | MRP操作 | 采购建议查看 |
|------|---------|---------|-------------|
| 生产计划员 | 增删改查 | 运行 | 是 |
| 生产总监 | 审批 | 只读 | 是 |
| 采购员 | 只读 | 只读 | 是（仅本人负责的物料） |
| 仓库管理员 | 只读 | 查看库存影响 | 否 |
| 销售经理 | 查看确认计划 | 不可见 | 不可见 |

### 4.3 异常处理
| 异常类型 | 处理方式 |
|----------|----------|
| BOM缺失 | MRP跳过该物料，标记警告 |
| 库存数据未更新 | 使用最近一次快照，提醒仓管同步 |
| 供应商价格无记录 | 使用历史均价，提示补录 |
| 产能严重不足（>120%） | 发出红色预警，建议分批次排程 |

---

## 5. 关键绩效指标（KPI）

| KPI | 定义 | 目标值 |
|-----|------|--------|
| 计划达成率 | 按期完成工单数 / 总工单数 | ≥90% |
| 库存周转天数 | 平均库存 / 日均消耗 | ≤30天 |
| MRP计算准确率 | 实际缺料次数 / MRP预测缺料数 | ≥95% |
| 订单准时交付率 | 按时交付订单数 / 总订单数 | ≥95% |
| 产能利用率 | 实际工时 / 可用工时 | 75%-85%（最佳区间） |

---

## 6. 实施路线图

| 阶段 | 时间 | 交付内容 | 验收标准 |
|------|------|----------|----------|
| Phase 1 | 第1-2周 | MPS计划创建+状态机 | 可创建/确认/下达计划，优先级排序正确 |
| Phase 2 | 第3-4周 | MRP计算+BOM展开 | BOM多层展开正确，净需求计算准确 |
| Phase 3 | 第5-6周 | 产能负荷分析 + 冲突检测 | 能识别超负荷工站，预警准确 |
| Phase 4 | 第7-8周 | MRP采购建议 + MES工单联动 | 短缺自动生成采购建议，release后转MES工单 |
| Phase 5 | 第9周 | 报表优化 + 权限配置 | 各角色看到正确数据，导出格式规范 |

---

*本文档为业务需求规格，后续将由技术团队据此开发接口实现。*